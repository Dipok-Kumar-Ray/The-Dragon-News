# 🚀 MERN স্ট্যাক গুগল ডকস রিডার - দ্রুত শুরুর গাইড

## 📋 প্রয়োজনীয় সফটওয়্যার
- **Node.js** 16+ [ডাউনলোড](https://nodejs.org/)
- **MongoDB** [ইনস্টল গাইড](https://docs.mongodb.com/manual/installation/)
- **Git** [ডাউনলোড](https://git-scm.com/)
- **কোড এডিটর** (VS Code সুপারিশকৃত)

## ⚡ ৫ মিনিটে সেটআপ

### ১. প্রজেক্ট ক্লোন করুন
```bash
git clone https://github.com/your-username/mern-google-docs.git
cd mern-google-docs
```

### ২. Dependencies ইনস্টল করুন
```bash
# সব dependencies একসাথে ইনস্টল
npm install && cd backend && npm install && cd ../frontend && npm install && cd ..
```

### ৩. MongoDB চালু করুন
```bash
# Local MongoDB (Ubuntu/Debian)
sudo systemctl start mongodb

# অথবা Docker
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

### ৪. Environment Variables সেট করুন
```bash
cd backend
cp .env.example .env
```

**গুরুত্বপূর্ণ**: `.env` ফাইলে Google credentials যোগ করুন:
```env
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
JWT_SECRET=your_very_secure_random_secret_key_here
```

### ৫. অ্যাপ্লিকেশন চালান
```bash
# Root directory থেকে
npm run dev
```

🎉 **সফল!** অ্যাপ্লিকেশন চালু হয়েছে:
- **Frontend**: http://localhost:3000
- **Backend**: http://localhost:5000

## 🔑 Google OAuth সেটআপ (অতিরিক্ত ৫ মিনিট)

### Google Cloud Console এ যান: https://console.cloud.google.com/

1. **নতুন প্রজেক্ট তৈরি** করুন
2. **APIs & Services > Library** এ যান
3. **Google Docs API** ও **Google Drive API** enable করুন
4. **OAuth consent screen** কনফিগার করুন
5. **Credentials** তৈরি করুন (Web Application)
6. **Authorized redirect URIs** যোগ করুন: `http://localhost:5000/api/auth/google/callback`

## 📱 প্রথম ব্যবহার

1. **http://localhost:3000** এ যান
2. **"শুরু করুন"** বাটনে ক্লিক করুন
3. **Google দিয়ে সাইন ইন** করুন
4. **Google Docs URL** পেস্ট করুন: 
   ```
   https://docs.google.com/document/d/1eAKrg7HjZGCvakBylkNu-dbgSX8ykd7F2oQ5PvoM0Hs/edit
   ```
5. **"ডকুমেন্ট পড়ুন"** ক্লিক করুন

## 🛠️ সাধারণ সমস্যা ও সমাধান

### MongoDB Connection Error
```bash
# সমাধান:
sudo systemctl status mongodb
sudo systemctl start mongodb
```

### Google OAuth Error
```bash
# চেক করুন:
- .env ফাইলে GOOGLE_CLIENT_ID আছে কি?
- Google Cloud Console এ redirect URI সঠিক আছে কি?
```

### Port Already in Use
```bash
# সমাধান:
lsof -ti:3000 | xargs kill -9
lsof -ti:5000 | xargs kill -9
```

## 📂 প্রজেক্ট স্ট্রাকচার

```
mern-google-docs/
├── 📁 backend/              # Node.js + Express API
│   ├── 📁 config/          # Database connection
│   ├── 📁 models/          # MongoDB schemas
│   ├── 📁 routes/          # API endpoints
│   ├── 📁 services/        # Google API integration
│   └── 📄 server.js        # Main server file
├── 📁 frontend/            # React.js UI
│   ├── 📁 src/
│   │   ├── 📁 components/  # React components
│   │   ├── 📁 pages/       # Page components
│   │   ├── 📁 store/       # State management
│   │   └── 📄 App.js       # Main App
│   └── 📄 package.json
└── 📄 package.json         # Root package
```

## 🚀 Development Commands

```bash
# Development (সব একসাথে)
npm run dev

# আলাদা আলাদা
npm run server    # Backend only
npm run client    # Frontend only

# Production build
npm run build

# Tests চালান
cd backend && npm test
cd frontend && npm test
```

## 🔧 Available Scripts

| Command | Description |
|---------|-------------|
| `npm run dev` | Frontend ও Backend একসাথে চালু |
| `npm run server` | শুধু Backend চালু |
| `npm run client` | শুধু Frontend চালু |
| `npm run build` | Production build |

## 💡 Next Steps

1. **Document Management**: Documents page এ যান
2. **Favorites**: প্রিয় documents mark করুন  
3. **Search**: Documents খুঁজুন
4. **Analytics**: Dashboard এ statistics দেখুন

## 🆘 সাহায্য দরকার?

- **Documentation**: `README.md` দেখুন
- **Detailed Guide**: `মূল-পদ্ধতি-ও-ব্যবহার.md` পড়ুন
- **Issues**: GitHub এ issue তৈরি করুন
- **Email**: your-email@example.com

## 🌟 Features

✅ **Google Docs Reading** - যেকোনো public Google Doc পড়ুন  
✅ **OAuth Authentication** - নিরাপদ Google login  
✅ **Document Storage** - MongoDB তে সংরক্ষণ  
✅ **Responsive UI** - মোবাইল-ফ্রেন্ডলি  
✅ **Bangla Interface** - সম্পূর্ণ বাংলা UI  
✅ **Real-time** - Live document updates  

---

**🎯 লক্ষ্য**: ৫ মিনিটে চালু, ১০ মিনিটে প্রোডাক্টিভ!