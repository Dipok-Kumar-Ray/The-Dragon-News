# 🍽️ সম্পূর্ণ খাদ্য দান ব্যবস্থাপনা সিস্টেম

## 📋 সূচিপত্র

1. [প্রকল্প পরিচিতি](#-প্রকল্প-পরিচিতি)
2. [মূল বৈশিষ্ট্যসমূহ](#-মূল-বৈশিষ্ট্যসমূহ)
3. [প্রযুক্তিগত স্ট্যাক](#-প্রযুক্তিগত-স্ট্যাক)
4. [ইনস্টলেশন গাইড](#-ইনস্টলেশন-গাইড)
5. [All Donations Page](#-all-donations-page)
6. [Donation Details Page](#-donation-details-page)
7. [API Documentation](#-api-documentation)
8. [Database Schema](#-database-schema)
9. [Component Architecture](#-component-architecture)
10. [State Management](#-state-management)
11. [Performance Optimization](#-performance-optimization)
12. [Security Implementation](#-security-implementation)
13. [Testing Guidelines](#-testing-guidelines)
14. [Deployment Guide](#-deployment-guide)
15. [Troubleshooting](#-troubleshooting)

---

## 🎯 প্রকল্প পরিচিতি

এই প্রকল্পটি একটি সম্পূর্ণ **MERN Stack** ভিত্তিক খাদ্য দান ব্যবস্থাপনা সিস্টেম যা রেস্তোরাঁ, দাতব্য সংস্থা এবং ব্যবহারকারীদের মধ্যে খাদ্য দানের কার্যক্রম সহজতর করে।

### 🎯 মূল উদ্দেশ্য
- খাদ্য অপচয় রোধ করা
- ক্ষুধার্ত মানুষদের খাদ্য সহায়তা প্রদান
- রেস্তোরাঁ ও দাতব্য সংস্থার মধ্যে সেতুবন্ধন তৈরি
- স্বচ্ছ ও নিরাপদ খাদ্য বিতরণ নিশ্চিতকরণ

---

## ✨ মূল বৈশিষ্ট্যসমূহ

### 🏪 রেস্তোরাঁর জন্য
- ✅ খাদ্য দান তৈরি ও ব্যবস্থাপনা
- ✅ রিকুয়েস্ট গ্রহণ ও অনুমোদন
- ✅ পিকআপ সময় নির্ধারণ
- ✅ দান পরিসংখ্যান দেখা
- ✅ রেটিং ও রিভিউ সিস্টেম

### 🏥 দাতব্য সংস্থার জন্য
- ✅ উপলব্ধ দান ব্রাউজ করা
- ✅ সার্চ ও ফিল্টার ব্যবহার
- ✅ দান রিকুয়েস্ট করা
- ✅ পিকআপ নিশ্চিতকরণ
- ✅ প্রিয় তালিকা ব্যবস্থাপনা

### 👤 সাধারণ ব্যবহারকারীর জন্য
- ✅ দান দেখা ও খোঁজা
- ✅ প্রিয় তালিকায় যোগ করা
- ✅ রিভিউ লেখা
- ✅ দাতব্য কার্যক্রম অনুসরণ

### 👨‍💼 অ্যাডমিনের জন্য
- ✅ দান যাচাইকরণ ও অনুমোদন
- ✅ ব্যবহারকারী ব্যবস্থাপনা
- ✅ সিস্টেম মনিটরিং
- ✅ রিপোর্ট জেনারেশন

---

## 🛠️ প্রযুক্তিগত স্ট্যাক

### Frontend (React.js)
```json
{
  "react": "^18.2.0",
  "react-router-dom": "^6.8.0",
  "tailwindcss": "^3.2.0",
  "daisyui": "^2.50.0",
  "framer-motion": "^9.0.0",
  "react-hot-toast": "^2.4.0",
  "react-icons": "^4.7.0",
  "axios": "^1.3.0",
  "recharts": "^2.5.0",
  "react-query": "^3.39.0"
}
```

### Backend (Node.js)
```json
{
  "express": "^4.18.0",
  "mongoose": "^6.9.0",
  "jsonwebtoken": "^9.0.0",
  "bcryptjs": "^2.4.3",
  "express-validator": "^6.14.0",
  "helmet": "^6.0.0",
  "cors": "^2.8.5",
  "express-rate-limit": "^6.7.0",
  "nodemailer": "^6.9.0",
  "multer": "^1.4.5"
}
```

### Database
- **MongoDB** - NoSQL ডাটাবেস
- **Mongoose** - ODM লাইব্রেরি
- **MongoDB Atlas** - ক্লাউড হোস্টিং

---

## 🚀 ইনস্টলেশন গাইড

### 📋 পূর্বশর্ত
```bash
# Node.js (v16 বা উপরে)
node --version

# npm বা yarn
npm --version

# MongoDB (লোকাল বা Atlas)
```

### 🔧 প্রজেক্ট সেটআপ

#### 1. Repository Clone করুন
```bash
git clone https://github.com/your-username/food-donation-app.git
cd food-donation-app
```

#### 2. Backend Setup
```bash
cd food-donation-backend

# Dependencies install করুন
npm install

# Environment variables setup
cp .env.example .env

# .env ফাইল এডিট করুন
PORT=5000
NODE_ENV=development
MONGO_URI=mongodb://localhost:27017/food-donation
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRE=30d
REFRESH_TOKEN_SECRET=your-refresh-token-secret
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Server start করুন
npm run dev
```

#### 3. Frontend Setup
```bash
cd ../food-donation-app

# Dependencies install করুন
npm install

# Environment variables setup
cp .env.example .env

# .env ফাইল এডিট করুন
REACT_APP_API_URL=http://localhost:5000/api
REACT_APP_APP_NAME=খাদ্য দান সিস্টেম

# Frontend start করুন
npm start
```

#### 4. Database Setup
```bash
# MongoDB server start করুন (যদি লোকাল হয়)
mongod

# অথবা MongoDB Atlas connection string ব্যবহার করুন
```

---

## 📑 All Donations Page

### 🎯 উদ্দেশ্য
সকল যাচাইকৃত এবং অনুমোদিত খাদ্য দান প্রদর্শন করা, উন্নত সার্চ এবং ফিল্টার সুবিধা সহ।

### 🔧 মূল Features

#### 1. **Advanced Search & Filter**
```javascript
// Search functionality
const handleSearch = (query) => {
  setSearchQuery(query);
  setCurrentPage(1);
  // API call with search parameter
};

// Location-based filtering
const handleLocationFilter = (location) => {
  setLocationFilter(location);
  setCurrentPage(1);
};

// Status filtering
const statusOptions = [
  { value: 'all', label: 'সব স্ট্যাটাস' },
  { value: 'available', label: 'উপলব্ধ' },
  { value: 'requested', label: 'রিকুয়েস্ট করা হয়েছে' },
  { value: 'picked_up', label: 'পিকআপ সম্পন্ন' }
];
```

#### 2. **Smart Sorting**
```javascript
const sortOptions = [
  { value: 'createdAt', label: 'তারিখ অনুযায়ী' },
  { value: 'quantity', label: 'পরিমাণ অনুযায়ী' },
  { value: 'pickupTimeStart', label: 'পিকআপ সময় অনুযায়ী' },
  { value: 'restaurantName', label: 'রেস্তোরাঁর নাম অনুযায়ী' }
];
```

#### 3. **Responsive Design**
- Grid View (Desktop/Tablet)
- List View (Mobile Optimization)
- Card-based Layout
- Infinite Scroll Support

#### 4. **Real-time Updates**
```javascript
useEffect(() => {
  // Auto-refresh every 30 seconds
  const interval = setInterval(loadDonations, 30000);
  return () => clearInterval(interval);
}, []);
```

### 📱 UI Components

#### DonationCard Component
```jsx
<DonationCard
  donation={donation}
  isFavorite={favorites.includes(donation._id)}
  onViewDetails={handleViewDetails}
  onFavoriteToggle={loadFavorites}
  viewMode={viewMode} // 'grid' | 'list'
/>
```

#### Features:
- 🖼️ Food image display
- 📍 Location information
- ⏰ Pickup time status
- 💖 Favorite toggle
- 👁️ View details button
- 🏷️ Status badges

---

## 📄 Donation Details Page

### 🎯 উদ্দেশ্য
একটি নির্দিষ্ট খাদ্য দানের সম্পূর্ণ তথ্য প্রদর্শন এবং বিভিন্ন Actions সম্পাদন।

### 🔧 মূল Features

#### 1. **Detailed Information Display**
```javascript
const donationInfo = {
  title: "Fresh Bread Donation",
  description: "২০ কেজি তাজা রুটি...",
  restaurantName: "ABC Restaurant",
  location: "ধানমন্ডি, ঢাকা",
  quantity: "২০ কেজি",
  pickupTimeWindow: "২:০০ PM - ৬:০০ PM",
  status: "available"
};
```

#### 2. **Role-based Actions**

##### For Users/Charities:
```jsx
// Save to Favorites
<button onClick={handleFavoriteToggle}>
  {isFavorite ? <FaHeart /> : <FaRegHeart />}
  প্রিয় তালিকায় {isFavorite ? 'আছে' : 'যোগ করুন'}
</button>
```

##### For Charities Only:
```jsx
// Request Donation
{user.role === 'charity' && donation.status === 'available' && (
  <button onClick={() => setShowRequestModal(true)}>
    <FaHandHolding />
    ডোনেশন রিকুয়েস্ট করুন
  </button>
)}

// Confirm Pickup (for accepted requests)
{canConfirmPickup && (
  <button onClick={handleConfirmPickup}>
    <FaCheckCircle />
    পিকআপ নিশ্চিত করুন
  </button>
)}
```

#### 3. **Request Modal**
```jsx
<RequestDonationModal
  donation={donation}
  isOpen={showRequestModal}
  onClose={() => setShowRequestModal(false)}
  onSubmit={handleRequestSubmit}
/>
```

Modal Fields:
- 📝 Donation title (readonly)
- 🏪 Restaurant name (readonly)
- 🏥 Charity name (readonly)
- 📧 Charity email (readonly)
- 📋 Request description (editable)
- 🕐 Pickup time selection

#### 4. **Review System**
```jsx
<ReviewSection
  donationId={donation._id}
  currentUserRole={user.role}
  onAddReview={handleAddReview}
/>
```

Features:
- ⭐ Star rating system (1-5)
- 💬 Text reviews
- 👤 Reviewer identification
- 📅 Review timestamps

---

## 🔌 API Documentation

### 🔐 Authentication Endpoints

#### POST /api/auth/login
```javascript
// Request
{
  "email": "user@example.com",
  "password": "password123"
}

// Response
{
  "success": true,
  "token": "jwt-token",
  "refreshToken": "refresh-token",
  "user": {
    "id": "user-id",
    "name": "User Name",
    "email": "user@example.com",
    "role": "charity"
  }
}
```

#### POST /api/auth/refresh
```javascript
// Request
{
  "refreshToken": "refresh-token"
}

// Response
{
  "success": true,
  "token": "new-jwt-token",
  "refreshToken": "new-refresh-token"
}
```

### 🍽️ Donation Endpoints

#### GET /api/donations
```javascript
// Query Parameters
{
  "page": 1,
  "limit": 12,
  "search": "রুটি",
  "location": "ধানমন্ডি",
  "status": "available",
  "sortBy": "createdAt",
  "sortOrder": "desc"
}

// Response
{
  "success": true,
  "donations": [...],
  "pagination": {
    "currentPage": 1,
    "totalPages": 5,
    "totalCount": 50,
    "limit": 12
  }
}
```

#### GET /api/donations/:id
```javascript
// Response
{
  "success": true,
  "donation": {
    "_id": "donation-id",
    "title": "Fresh Bread Donation",
    "description": "২০ কেজি তাজা রুটি",
    "restaurantId": {
      "name": "ABC Restaurant",
      "email": "restaurant@example.com"
    },
    "requests": [...],
    "reviews": [...]
  }
}
```

#### POST /api/donations/request
```javascript
// Request (Charity only)
{
  "donationId": "donation-id",
  "requestDescription": "আমাদের দরিদ্র শিশুদের জন্য প্রয়োজন",
  "pickupTime": "2024-01-15T15:00:00Z"
}

// Response
{
  "success": true,
  "message": "ডোনেশন রিকুয়েস্ট সফলভাবে পাঠানো হয়েছে",
  "request": {
    "_id": "request-id",
    "status": "pending",
    "createdAt": "2024-01-15T10:00:00Z"
  }
}
```

#### PATCH /api/donations/:id/status
```javascript
// Request
{
  "status": "picked_up"
}

// Response
{
  "success": true,
  "message": "ডোনেশনের স্ট্যাটাস আপডেট হয়েছে",
  "donation": {
    "_id": "donation-id",
    "status": "picked_up",
    "updatedAt": "2024-01-15T16:00:00Z"
  }
}
```

### 📊 Statistics Endpoints

#### GET /api/donations/stats
```javascript
// Query Parameters (Restaurant/Admin only)
{
  "timeRange": "month", // week, month, quarter, year
  "restaurantId": "restaurant-id" // optional
}

// Response
{
  "success": true,
  "summary": {
    "total": 150,
    "completed": 120,
    "totalQuantity": 2500,
    "requests": 85
  },
  "chartData": {
    "monthly": [...],
    "byType": [...],
    "daily": [...]
  },
  "topFoodTypes": [...],
  "recentActivity": [...]
}
```

---

## 🗄️ Database Schema

### 👤 User Collection
```javascript
{
  "_id": ObjectId,
  "name": String,
  "email": String (unique),
  "password": String (hashed),
  "role": "user|charity|restaurant|admin",
  "organizationName": String, // for charity/restaurant
  "phone": String,
  "location": String,
  "verified": Boolean,
  "avatar": String,
  "createdAt": Date,
  "updatedAt": Date
}
```

### 🍽️ Donation Collection
```javascript
{
  "_id": ObjectId,
  "title": String,
  "description": String,
  "foodType": String,
  "quantity": String,
  "restaurantId": ObjectId (ref: User),
  "restaurantName": String,
  "location": String,
  "coordinates": {
    "latitude": Number,
    "longitude": Number
  },
  "pickupTimeStart": Date,
  "pickupTimeEnd": Date,
  "pickupInstructions": String,
  "status": "pending|available|requested|accepted|picked_up|expired",
  "verified": Boolean,
  "approved": Boolean,
  "assignedCharity": ObjectId (ref: User),
  "image": String,
  "requests": [ObjectId] (ref: DonationRequest),
  "reviews": [ObjectId] (ref: Review),
  "viewCount": Number,
  "favoriteCount": Number,
  "createdAt": Date,
  "updatedAt": Date
}
```

### 📝 DonationRequest Collection
```javascript
{
  "_id": ObjectId,
  "donationId": ObjectId (ref: Donation),
  "charityId": ObjectId (ref: User),
  "charityName": String,
  "charityEmail": String,
  "requestDescription": String,
  "pickupTime": Date,
  "status": "pending|accepted|rejected|cancelled|completed",
  "responseMessage": String,
  "respondedBy": ObjectId (ref: User),
  "respondedAt": Date,
  "pickupConfirmation": {
    "confirmed": Boolean,
    "confirmedAt": Date,
    "confirmedBy": ObjectId,
    "confirmationCode": String
  },
  "createdAt": Date,
  "updatedAt": Date
}
```

### ⭐ Review Collection
```javascript
{
  "_id": ObjectId,
  "donationId": ObjectId (ref: Donation),
  "reviewerId": ObjectId (ref: User),
  "reviewerName": String,
  "reviewerRole": String,
  "rating": Number (1-5),
  "comment": String,
  "helpful": Number,
  "reported": Boolean,
  "createdAt": Date,
  "updatedAt": Date
}
```

### 💖 Favorite Collection
```javascript
{
  "_id": ObjectId,
  "userId": ObjectId (ref: User),
  "donationId": ObjectId (ref: Donation),
  "createdAt": Date
}
```

---

## 🏗️ Component Architecture

### 📁 Frontend Folder Structure
```
src/
├── components/
│   ├── Cards/
│   │   ├── DonationCard.jsx
│   │   └── ReviewCard.jsx
│   ├── Charts/
│   │   └── DonationStatistics.jsx
│   ├── Modals/
│   │   ├── RequestDonationModal.jsx
│   │   └── AddReviewModal.jsx
│   ├── UI/
│   │   ├── LoadingSpinner.jsx
│   │   ├── ErrorMessage.jsx
│   │   └── Pagination.jsx
│   └── Layout/
│       ├── Header.jsx
│       ├── Footer.jsx
│       └── Sidebar.jsx
├── pages/
│   ├── AllDonations.jsx
│   ├── DonationDetails.jsx
│   ├── Dashboard.jsx
│   └── Auth/
│       ├── Login.jsx
│       └── Register.jsx
├── hooks/
│   ├── useAuth.js
│   ├── useDonations.js
│   ├── useFavorites.js
│   └── useReviews.js
├── utils/
│   ├── axiosConfig.js
│   ├── helpers.js
│   └── constants.js
└── styles/
    ├── index.css
    └── tailwind.config.js
```

### 🔗 Component Relationships

#### Parent-Child Hierarchy
```
App.jsx
├── Header.jsx
├── AllDonations.jsx
│   ├── DonationCard.jsx (multiple)
│   ├── Pagination.jsx
│   └── LoadingSpinner.jsx
├── DonationDetails.jsx
│   ├── RequestDonationModal.jsx
│   ├── AddReviewModal.jsx
│   └── ReviewSection.jsx
└── Footer.jsx
```

#### Data Flow
```
API Layer (axiosConfig.js)
    ↓
Custom Hooks (useDonations.js)
    ↓
Page Components (AllDonations.jsx)
    ↓
Child Components (DonationCard.jsx)
    ↓
UI Components (Buttons, Modals)
```

---

## 🔄 State Management

### 📊 Local State (useState)
```javascript
// AllDonations.jsx
const [donations, setDonations] = useState([]);
const [loading, setLoading] = useState(true);
const [error, setError] = useState(null);
const [searchQuery, setSearchQuery] = useState('');
const [currentPage, setCurrentPage] = useState(1);
```

### 🌐 Global State (Context API)
```javascript
// AuthContext.js
export const AuthContext = createContext();

export const AuthProvider = ({ children }) => {
  const [user, setUser] = useState(null);
  const [isAuthenticated, setIsAuthenticated] = useState(false);
  
  return (
    <AuthContext.Provider value={{
      user,
      isAuthenticated,
      login,
      logout,
      register
    }}>
      {children}
    </AuthContext.Provider>
  );
};
```

### 🔄 Server State (React Query)
```javascript
// useDonations.js
export const useDonations = () => {
  const { data, isLoading, error, refetch } = useQuery(
    ['donations', filters],
    () => fetchDonations(filters),
    {
      staleTime: 5 * 60 * 1000, // 5 minutes
      cacheTime: 10 * 60 * 1000, // 10 minutes
    }
  );

  return { donations: data, loading: isLoading, error, refetch };
};
```

### 🎯 State Management Best Practices

#### 1. **Separation of Concerns**
- UI State: Component-level (useState)
- Server State: React Query
- Global State: Context API
- Form State: Custom hooks

#### 2. **Optimization Strategies**
```javascript
// Memoization
const memoizedDonations = useMemo(() => {
  return donations.filter(donation => 
    donation.title.toLowerCase().includes(searchQuery.toLowerCase())
  );
}, [donations, searchQuery]);

// Callback optimization
const handleSearch = useCallback((query) => {
  setSearchQuery(query);
  setCurrentPage(1);
}, []);

// Effect cleanup
useEffect(() => {
  const interval = setInterval(loadDonations, 30000);
  return () => clearInterval(interval);
}, [loadDonations]);
```

---

## ⚡ Performance Optimization

### 🚀 Frontend Optimizations

#### 1. **Code Splitting**
```javascript
// Lazy loading pages
const AllDonations = lazy(() => import('./pages/AllDonations'));
const DonationDetails = lazy(() => import('./pages/DonationDetails'));

// Usage in App.js
<Suspense fallback={<LoadingSpinner />}>
  <Routes>
    <Route path="/donations" element={<AllDonations />} />
    <Route path="/donations/:id" element={<DonationDetails />} />
  </Routes>
</Suspense>
```

#### 2. **Image Optimization**
```javascript
// Lazy loading images
const LazyImage = ({ src, alt, className }) => {
  const [loaded, setLoaded] = useState(false);
  const [error, setError] = useState(false);

  return (
    <div className={`relative ${className}`}>
      {!loaded && !error && (
        <div className="absolute inset-0 bg-gray-200 animate-pulse" />
      )}
      <img
        src={src}
        alt={alt}
        onLoad={() => setLoaded(true)}
        onError={() => setError(true)}
        className={`transition-opacity duration-300 ${
          loaded ? 'opacity-100' : 'opacity-0'
        }`}
      />
    </div>
  );
};
```

#### 3. **Virtual Scrolling**
```javascript
// For large lists
import { FixedSizeList as List } from 'react-window';

const DonationsList = ({ donations }) => {
  const Row = ({ index, style }) => (
    <div style={style}>
      <DonationCard donation={donations[index]} />
    </div>
  );

  return (
    <List
      height={600}
      itemCount={donations.length}
      itemSize={200}
    >
      {Row}
    </List>
  );
};
```

### 🔧 Backend Optimizations

#### 1. **Database Indexing**
```javascript
// Compound indexes for better query performance
donationSchema.index({ 
  verified: 1, 
  approved: 1, 
  status: 1,
  pickupTimeEnd: 1 
});

// Text index for search
donationSchema.index({
  title: 'text',
  description: 'text',
  foodType: 'text',
  location: 'text'
});
```

#### 2. **Aggregation Pipeline Optimization**
```javascript
const optimizedAggregation = [
  // Match first to reduce documents
  { $match: { verified: true, approved: true } },
  
  // Lookup only necessary fields
  {
    $lookup: {
      from: 'users',
      localField: 'restaurantId',
      foreignField: '_id',
      as: 'restaurant',
      pipeline: [
        { $project: { name: 1, organizationName: 1, location: 1 } }
      ]
    }
  },
  
  // Project to limit data transfer
  {
    $project: {
      title: 1,
      description: 1,
      quantity: 1,
      status: 1,
      createdAt: 1,
      'restaurant.name': 1
    }
  }
];
```

#### 3. **Caching Strategy**
```javascript
const NodeCache = require('node-cache');
const cache = new NodeCache({ stdTTL: 600 }); // 10 minutes

const getCachedDonations = async (cacheKey, fetchFunction) => {
  let data = cache.get(cacheKey);
  
  if (!data) {
    data = await fetchFunction();
    cache.set(cacheKey, data);
  }
  
  return data;
};
```

#### 4. **Pagination Optimization**
```javascript
// Cursor-based pagination for better performance
const getCursorPaginatedDonations = async (cursor, limit) => {
  const filter = cursor 
    ? { _id: { $gt: cursor }, verified: true, approved: true }
    : { verified: true, approved: true };

  const donations = await Donation
    .find(filter)
    .sort({ _id: 1 })
    .limit(limit + 1);

  const hasNext = donations.length > limit;
  if (hasNext) donations.pop();

  return {
    donations,
    nextCursor: hasNext ? donations[donations.length - 1]._id : null,
    hasNext
  };
};
```

---

## 🔒 Security Implementation

### 🛡️ Authentication & Authorization

#### 1. **JWT Token Management**
```javascript
// Token generation
const generateTokens = (userId) => {
  const payload = { id: userId };
  
  const accessToken = jwt.sign(payload, process.env.JWT_SECRET, {
    expiresIn: '15m'
  });
  
  const refreshToken = jwt.sign(payload, process.env.REFRESH_TOKEN_SECRET, {
    expiresIn: '7d'
  });
  
  return { accessToken, refreshToken };
};

// Token verification middleware
const verifyToken = (req, res, next) => {
  const token = req.header('Authorization')?.replace('Bearer ', '');
  
  if (!token) {
    return res.status(401).json({ message: 'Access denied' });
  }
  
  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    req.user = decoded;
    next();
  } catch (error) {
    res.status(401).json({ message: 'Invalid token' });
  }
};
```

#### 2. **Role-based Access Control**
```javascript
const roleAuth = (allowedRoles) => {
  return (req, res, next) => {
    if (!allowedRoles.includes(req.user.role)) {
      return res.status(403).json({
        message: 'Access denied for this role'
      });
    }
    next();
  };
};

// Usage
router.post('/donations', auth, roleAuth(['restaurant']), createDonation);
router.post('/request', auth, roleAuth(['charity']), requestDonation);
```

#### 3. **Input Validation & Sanitization**
```javascript
const { body, validationResult } = require('express-validator');

const donationValidation = [
  body('title')
    .trim()
    .isLength({ min: 3, max: 100 })
    .escape(),
  body('description')
    .trim()
    .isLength({ min: 10, max: 1000 })
    .escape(),
  body('pickupTimeStart')
    .isISO8601()
    .custom((value) => {
      if (new Date(value) <= new Date()) {
        throw new Error('পিকআপ সময় ভবিষ্যতে হতে হবে');
      }
      return true;
    })
];
```

### 🔐 Data Protection

#### 1. **Password Security**
```javascript
const bcrypt = require('bcryptjs');

// Password hashing
const hashPassword = async (password) => {
  const salt = await bcrypt.genSalt(12);
  return bcrypt.hash(password, salt);
};

// Password comparison
const comparePassword = async (password, hashedPassword) => {
  return bcrypt.compare(password, hashedPassword);
};
```

#### 2. **API Rate Limiting**
```javascript
const rateLimit = require('express-rate-limit');

const createAccountLimiter = rateLimit({
  windowMs: 60 * 60 * 1000, // 1 hour
  max: 5, // 5 accounts per hour
  message: 'অনেক বেশি অ্যাকাউন্ট তৈরির চেষ্টা',
  skip: (req) => req.ip === '127.0.0.1' // Skip localhost
});

const generalLimiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 minutes
  max: 100, // 100 requests per 15 minutes
  message: 'অনেক বেশি অনুরোধ'
});
```

#### 3. **CORS Configuration**
```javascript
const cors = require('cors');

const corsOptions = {
  origin: function (origin, callback) {
    const allowedOrigins = [
      'http://localhost:3000',
      'https://yourdomain.com'
    ];
    
    if (!origin || allowedOrigins.indexOf(origin) !== -1) {
      callback(null, true);
    } else {
      callback(new Error('CORS policy violation'));
    }
  },
  credentials: true,
  optionsSuccessStatus: 200
};

app.use(cors(corsOptions));
```

#### 4. **Helmet Security Headers**
```javascript
const helmet = require('helmet');

app.use(helmet({
  contentSecurityPolicy: {
    directives: {
      defaultSrc: ["'self'"],
      styleSrc: ["'self'", "'unsafe-inline'", "https://fonts.googleapis.com"],
      fontSrc: ["'self'", "https://fonts.gstatic.com"],
      imgSrc: ["'self'", "data:", "https:"],
      scriptSrc: ["'self'"]
    }
  },
  hsts: {
    maxAge: 31536000,
    includeSubDomains: true,
    preload: true
  }
}));
```

---

## 🧪 Testing Guidelines

### 🎯 Frontend Testing

#### 1. **Component Testing with React Testing Library**
```javascript
// DonationCard.test.js
import { render, screen, fireEvent } from '@testing-library/react';
import { BrowserRouter } from 'react-router-dom';
import DonationCard from '../DonationCard';

const mockDonation = {
  _id: '1',
  title: 'Test Donation',
  restaurantName: 'Test Restaurant',
  quantity: '10 kg',
  status: 'available'
};

const renderWithRouter = (component) => {
  return render(
    <BrowserRouter>
      {component}
    </BrowserRouter>
  );
};

describe('DonationCard', () => {
  test('renders donation information correctly', () => {
    renderWithRouter(
      <DonationCard 
        donation={mockDonation}
        onViewDetails={jest.fn()}
      />
    );
    
    expect(screen.getByText('Test Donation')).toBeInTheDocument();
    expect(screen.getByText('Test Restaurant')).toBeInTheDocument();
    expect(screen.getByText('10 kg')).toBeInTheDocument();
  });

  test('calls onViewDetails when details button is clicked', () => {
    const mockOnViewDetails = jest.fn();
    
    renderWithRouter(
      <DonationCard 
        donation={mockDonation}
        onViewDetails={mockOnViewDetails}
      />
    );
    
    fireEvent.click(screen.getByText('বিস্তারিত'));
    expect(mockOnViewDetails).toHaveBeenCalledWith('1');
  });
});
```

#### 2. **Custom Hook Testing**
```javascript
// useDonations.test.js
import { renderHook, waitFor } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from 'react-query';
import { useDonations } from '../useDonations';

const createWrapper = () => {
  const queryClient = new QueryClient({
    defaultOptions: {
      queries: { retry: false },
      mutations: { retry: false }
    }
  });

  return ({ children }) => (
    <QueryClientProvider client={queryClient}>
      {children}
    </QueryClientProvider>
  );
};

describe('useDonations', () => {
  test('fetches donations successfully', async () => {
    const { result } = renderHook(() => useDonations(), {
      wrapper: createWrapper()
    });

    await waitFor(() => {
      expect(result.current.isLoading).toBe(false);
    });

    expect(result.current.donations).toBeDefined();
  });
});
```

#### 3. **Integration Testing**
```javascript
// AllDonations.integration.test.js
import { render, screen, waitFor } from '@testing-library/react';
import { rest } from 'msw';
import { setupServer } from 'msw/node';
import AllDonations from '../AllDonations';

// Mock API server
const server = setupServer(
  rest.get('/api/donations', (req, res, ctx) => {
    return res(ctx.json({
      success: true,
      donations: [mockDonation],
      pagination: { totalCount: 1, totalPages: 1 }
    }));
  })
);

beforeAll(() => server.listen());
afterEach(() => server.resetHandlers());
afterAll(() => server.close());

describe('AllDonations Integration', () => {
  test('loads and displays donations from API', async () => {
    render(<AllDonations />);
    
    expect(screen.getByText('ডোনেশন লোড হচ্ছে...')).toBeInTheDocument();
    
    await waitFor(() => {
      expect(screen.getByText('Test Donation')).toBeInTheDocument();
    });
  });
});
```

### 🔧 Backend Testing

#### 1. **Unit Testing with Jest**
```javascript
// donationsController.test.js
const { getAllDonations } = require('../controllers/donationsController');
const Donation = require('../models/Donation');

jest.mock('../models/Donation');

describe('Donations Controller', () => {
  describe('getAllDonations', () => {
    test('returns paginated donations successfully', async () => {
      const mockReq = {
        query: { page: 1, limit: 10 }
      };
      const mockRes = {
        json: jest.fn(),
        status: jest.fn().mockReturnThis()
      };

      const mockDonations = [{ title: 'Test Donation' }];
      Donation.aggregate.mockResolvedValue([{
        donations: mockDonations,
        totalCount: [{ count: 1 }]
      }]);

      await getAllDonations(mockReq, mockRes);

      expect(mockRes.json).toHaveBeenCalledWith({
        success: true,
        donations: mockDonations,
        pagination: expect.any(Object)
      });
    });
  });
});
```

#### 2. **API Endpoint Testing with Supertest**
```javascript
// donations.integration.test.js
const request = require('supertest');
const app = require('../app');
const mongoose = require('mongoose');

describe('Donations API', () => {
  beforeAll(async () => {
    await mongoose.connect(process.env.TEST_MONGO_URI);
  });

  afterAll(async () => {
    await mongoose.connection.close();
  });

  describe('GET /api/donations', () => {
    test('returns donations list for authenticated user', async () => {
      const token = generateTestToken();
      
      const response = await request(app)
        .get('/api/donations')
        .set('Authorization', `Bearer ${token}`)
        .expect(200);

      expect(response.body.success).toBe(true);
      expect(response.body.donations).toBeDefined();
    });

    test('returns 401 for unauthenticated request', async () => {
      await request(app)
        .get('/api/donations')
        .expect(401);
    });
  });
});
```

#### 3. **Database Testing**
```javascript
// models/Donation.test.js
const mongoose = require('mongoose');
const Donation = require('../Donation');

describe('Donation Model', () => {
  beforeAll(async () => {
    await mongoose.connect(process.env.TEST_MONGO_URI);
  });

  afterEach(async () => {
    await Donation.deleteMany({});
  });

  afterAll(async () => {
    await mongoose.connection.close();
  });

  test('creates a valid donation', async () => {
    const donationData = {
      title: 'Test Donation',
      description: 'Test description for donation',
      foodType: 'ভাত ও তরকারি',
      quantity: '10 kg',
      restaurantId: new mongoose.Types.ObjectId(),
      location: 'Test Location',
      pickupTimeStart: new Date(Date.now() + 3600000),
      pickupTimeEnd: new Date(Date.now() + 7200000)
    };

    const donation = new Donation(donationData);
    const savedDonation = await donation.save();

    expect(savedDonation._id).toBeDefined();
    expect(savedDonation.title).toBe('Test Donation');
    expect(savedDonation.status).toBe('pending');
  });

  test('validation fails for invalid data', async () => {
    const donation = new Donation({});
    
    let error;
    try {
      await donation.save();
    } catch (err) {
      error = err;
    }

    expect(error).toBeDefined();
    expect(error.errors.title).toBeDefined();
  });
});
```

### 📊 Test Coverage

#### Setup Jest Configuration
```javascript
// jest.config.js
module.exports = {
  testEnvironment: 'jsdom',
  setupFilesAfterEnv: ['<rootDir>/src/setupTests.js'],
  moduleNameMapping: {
    '\\.(css|less|scss|sass)$': 'identity-obj-proxy'
  },
  collectCoverageFrom: [
    'src/**/*.{js,jsx}',
    '!src/index.js',
    '!src/setupTests.js'
  ],
  coverageThreshold: {
    global: {
      branches: 80,
      functions: 80,
      lines: 80,
      statements: 80
    }
  }
};
```

#### Run Tests
```bash
# Frontend tests
npm test

# Backend tests
npm run test:backend

# Coverage report
npm run test:coverage

# E2E tests with Cypress
npm run test:e2e
```

---

## 🚀 Deployment Guide

### 🌐 Frontend Deployment (Vercel/Netlify)

#### 1. **Build Optimization**
```bash
# Create production build
npm run build

# Analyze bundle size
npm install -g webpack-bundle-analyzer
npx webpack-bundle-analyzer build/static/js/*.js
```

#### 2. **Environment Variables**
```bash
# .env.production
REACT_APP_API_URL=https://your-api-domain.com/api
REACT_APP_APP_NAME=খাদ্য দান সিস্টেম
REACT_APP_ENVIRONMENT=production
```

#### 3. **Vercel Deployment**
```json
// vercel.json
{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "routes": [
    {
      "src": "/static/(.*)",
      "headers": {
        "cache-control": "public, max-age=31536000, immutable"
      }
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
```

### 🔧 Backend Deployment (Railway/Heroku/DigitalOcean)

#### 1. **Production Dependencies**
```json
// package.json
{
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
```

#### 2. **Environment Configuration**
```bash
# Production environment variables
NODE_ENV=production
PORT=5000
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/production
JWT_SECRET=super-secure-secret-key-for-production
JWT_EXPIRE=30d
CORS_ORIGIN=https://your-frontend-domain.com
```

#### 3. **Railway Deployment**
```yaml
# railway.toml
[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
restartPolicyType = "always"

[[services]]
name = "food-donation-api"
source = "."
```

#### 4. **Docker Configuration**
```dockerfile
# Dockerfile
FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci --only=production

# Copy source code
COPY . .

# Expose port
EXPOSE 5000

# Health check
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
  CMD curl -f http://localhost:5000/health || exit 1

# Start application
CMD ["npm", "start"]
```

### 🗄️ Database Deployment (MongoDB Atlas)

#### 1. **Database Setup**
```javascript
// config/database.js
const mongoose = require('mongoose');

const connectDB = async () => {
  try {
    const options = {
      useNewUrlParser: true,
      useUnifiedTopology: true,
      maxPoolSize: 10,
      serverSelectionTimeoutMS: 5000,
      socketTimeoutMS: 45000,
    };

    if (process.env.NODE_ENV === 'production') {
      options.ssl = true;
      options.sslValidate = true;
    }

    const conn = await mongoose.connect(process.env.MONGO_URI, options);
    console.log(`MongoDB Connected: ${conn.connection.host}`);
  } catch (error) {
    console.error('Database connection error:', error);
    process.exit(1);
  }
};

module.exports = connectDB;
```

#### 2. **Database Indexing for Production**
```javascript
// Create indexes for production
const createIndexes = async () => {
  try {
    // Donation indexes
    await Donation.collection.createIndex({ 
      verified: 1, 
      approved: 1, 
      status: 1 
    });
    
    await Donation.collection.createIndex({ 
      location: 'text', 
      title: 'text', 
      description: 'text' 
    });

    // User indexes
    await User.collection.createIndex({ email: 1 }, { unique: true });
    await User.collection.createIndex({ role: 1, verified: 1 });

    console.log('Database indexes created successfully');
  } catch (error) {
    console.error('Error creating indexes:', error);
  }
};
```

### 📊 Monitoring & Analytics

#### 1. **Application Monitoring**
```javascript
// middleware/monitoring.js
const morgan = require('morgan');

// Custom logging format
const logFormat = process.env.NODE_ENV === 'production' 
  ? 'combined' 
  : 'dev';

app.use(morgan(logFormat));

// Error tracking
const errorHandler = (err, req, res, next) => {
  console.error('Error:', {
    message: err.message,
    stack: err.stack,
    url: req.url,
    method: req.method,
    ip: req.ip,
    userAgent: req.get('User-Agent')
  });

  res.status(err.statusCode || 500).json({
    success: false,
    message: process.env.NODE_ENV === 'production' 
      ? 'Server Error' 
      : err.message
  });
};
```

#### 2. **Performance Monitoring**
```javascript
// middleware/performance.js
const performanceMonitor = (req, res, next) => {
  const start = Date.now();
  
  res.on('finish', () => {
    const duration = Date.now() - start;
    
    if (duration > 1000) { // Log slow requests
      console.warn('Slow request:', {
        method: req.method,
        url: req.url,
        duration: `${duration}ms`,
        status: res.statusCode
      });
    }
  });
  
  next();
};
```

---

## 🔧 Troubleshooting

### 🐛 Common Issues & Solutions

#### 1. **CORS Errors**
```javascript
// Problem: CORS policy blocking requests
// Solution: Configure CORS properly

const corsOptions = {
  origin: [
    'http://localhost:3000',
    'https://your-frontend-domain.com'
  ],
  credentials: true,
  methods: ['GET', 'POST', 'PUT', 'DELETE', 'PATCH'],
  allowedHeaders: ['Content-Type', 'Authorization']
};

app.use(cors(corsOptions));
```

#### 2. **JWT Token Issues**
```javascript
// Problem: Token expiration or invalid token
// Solution: Implement token refresh mechanism

const handleTokenError = (error) => {
  if (error.name === 'TokenExpiredError') {
    // Attempt token refresh
    return refreshToken();
  } else if (error.name === 'JsonWebTokenError') {
    // Redirect to login
    redirectToLogin();
  }
};
```

#### 3. **Database Connection Issues**
```javascript
// Problem: MongoDB connection timeout
// Solution: Implement connection retry logic

const connectWithRetry = async () => {
  const maxRetries = 5;
  let retries = 0;

  while (retries < maxRetries) {
    try {
      await mongoose.connect(process.env.MONGO_URI, {
        serverSelectionTimeoutMS: 5000,
        connectTimeoutMS: 10000,
      });
      console.log('Database connected successfully');
      return;
    } catch (error) {
      retries++;
      console.log(`Database connection attempt ${retries} failed`);
      
      if (retries === maxRetries) {
        console.error('Max retries reached. Exiting...');
        process.exit(1);
      }
      
      // Wait before retry
      await new Promise(resolve => setTimeout(resolve, 5000));
    }
  }
};
```

#### 4. **Memory Leaks**
```javascript
// Problem: Memory usage increasing over time
// Solution: Proper cleanup and monitoring

// Cleanup event listeners
useEffect(() => {
  const handleResize = () => setWindowWidth(window.innerWidth);
  window.addEventListener('resize', handleResize);
  
  return () => {
    window.removeEventListener('resize', handleResize);
  };
}, []);

// Monitor memory usage
if (process.env.NODE_ENV === 'development') {
  setInterval(() => {
    const usage = process.memoryUsage();
    console.log('Memory usage:', {
      rss: `${Math.round(usage.rss / 1024 / 1024)}MB`,
      heapUsed: `${Math.round(usage.heapUsed / 1024 / 1024)}MB`
    });
  }, 60000);
}
```

#### 5. **Slow API Responses**
```javascript
// Problem: API endpoints taking too long
// Solution: Optimize queries and add caching

// Optimize aggregation pipeline
const optimizedQuery = Donation.aggregate([
  // Match first to reduce dataset
  { $match: { verified: true, approved: true } },
  
  // Limit fields in lookup
  {
    $lookup: {
      from: 'users',
      localField: 'restaurantId',
      foreignField: '_id',
      as: 'restaurant',
      pipeline: [{ $project: { name: 1, location: 1 } }]
    }
  },
  
  // Project only necessary fields
  {
    $project: {
      title: 1,
      quantity: 1,
      status: 1,
      'restaurant.name': 1
    }
  }
]);
```

### 📱 Mobile Responsiveness Issues

#### 1. **Touch Events**
```css
/* Fix touch scrolling on iOS */
.scrollable-container {
  -webkit-overflow-scrolling: touch;
  overflow-y: auto;
}

/* Prevent zoom on input focus */
input, textarea, select {
  font-size: 16px;
}
```

#### 2. **Viewport Configuration**
```html
<!-- Ensure proper mobile viewport -->
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
```

### 🔍 Debugging Tools

#### 1. **Frontend Debugging**
```javascript
// React Developer Tools usage
// Component state inspection
if (process.env.NODE_ENV === 'development') {
  window.debugDonations = donations;
  window.debugFilters = filters;
}

// Network request debugging
const debugAxios = axios.create({
  baseURL: process.env.REACT_APP_API_URL
});

debugAxios.interceptors.request.use(request => {
  console.log('🚀 Request:', request);
  return request;
});

debugAxios.interceptors.response.use(
  response => {
    console.log('✅ Response:', response);
    return response;
  },
  error => {
    console.error('❌ Error:', error);
    return Promise.reject(error);
  }
);
```

#### 2. **Backend Debugging**
```javascript
// Debug middleware
const debugMiddleware = (req, res, next) => {
  console.log(`${req.method} ${req.path}`, {
    query: req.query,
    body: req.body,
    user: req.user?.id,
    ip: req.ip
  });
  next();
};

// Database query debugging
mongoose.set('debug', process.env.NODE_ENV === 'development');
```

---

## 📈 Future Enhancements

### 🎯 Planned Features

#### 1. **Real-time Notifications**
```javascript
// Socket.io implementation
const io = require('socket.io')(server, {
  cors: {
    origin: process.env.CLIENT_URL,
    methods: ["GET", "POST"]
  }
});

// Real-time donation updates
io.on('connection', (socket) => {
  socket.on('joinDonationRoom', (donationId) => {
    socket.join(`donation-${donationId}`);
  });
});

// Emit updates when donation status changes
const notifyDonationUpdate = (donationId, update) => {
  io.to(`donation-${donationId}`).emit('donationUpdated', update);
};
```

#### 2. **Geolocation Features**
```javascript
// Location-based search
const findNearbyDonations = async (userLocation, radius = 10) => {
  return await Donation.find({
    coordinates: {
      $near: {
        $geometry: {
          type: "Point",
          coordinates: [userLocation.longitude, userLocation.latitude]
        },
        $maxDistance: radius * 1000 // Convert km to meters
      }
    },
    status: 'available'
  });
};
```

#### 3. **AI-powered Matching**
```javascript
// Smart donation-charity matching
const getRecommendedDonations = async (charityId) => {
  const charity = await User.findById(charityId);
  const preferences = charity.preferences;
  
  return await Donation.find({
    foodType: { $in: preferences.preferredFoodTypes },
    location: { $regex: preferences.location, $options: 'i' },
    status: 'available'
  }).sort({ matchScore: -1 });
};
```

#### 4. **Advanced Analytics**
```javascript
// ML-based insights
const generateInsights = async (restaurantId) => {
  const donations = await Donation.find({ restaurantId });
  
  return {
    peakDonationTimes: analyzePeakTimes(donations),
    foodTypePreferences: analyzeFoodTypes(donations),
    optimalQuantities: analyzeQuantities(donations),
    seasonalTrends: analyzeSeasonalData(donations)
  };
};
```

### 🔧 Technical Improvements

#### 1. **Microservices Architecture**
- User Service
- Donation Service  
- Notification Service
- Analytics Service
- File Upload Service

#### 2. **Advanced Caching**
- Redis implementation
- CDN integration
- Service worker caching

#### 3. **Progressive Web App**
- Offline functionality
- Push notifications
- Install prompts

---

## 📞 Support & Contact

### 🆘 Getting Help

#### 1. **Documentation**
- README.md ফাইল পড়ুন
- API documentation দেখুন
- Code comments অনুসরণ করুন

#### 2. **Common Commands**
```bash
# Development
npm run dev          # Start development server
npm test            # Run tests
npm run build       # Create production build

# Database
npm run seed        # Seed sample data
npm run migrate     # Run migrations
npm run reset-db    # Reset database

# Deployment
npm run deploy      # Deploy to production
npm run logs        # View production logs
```

#### 3. **Useful Links**
- [React Documentation](https://reactjs.org/docs)
- [Node.js Documentation](https://nodejs.org/docs)
- [MongoDB Documentation](https://docs.mongodb.com)
- [Tailwind CSS](https://tailwindcss.com/docs)

---

## 📄 License

This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.

---

## 🙏 Acknowledgments

- React.js community
- Node.js ecosystem
- MongoDB team
- Tailwind CSS creators
- All contributors and supporters

---

**Made with ❤️ for a better world through reduced food waste**

---

## 📝 Changelog

### Version 1.0.0 (Current)
- ✅ Complete MERN stack implementation
- ✅ All Donations page with search & filters
- ✅ Donation Details page with full functionality
- ✅ Role-based access control
- ✅ Request system for charities
- ✅ Review and rating system
- ✅ Statistics dashboard
- ✅ Responsive design
- ✅ Error handling and validation
- ✅ Performance optimizations

### Upcoming Version 1.1.0
- 🔄 Real-time notifications
- 🔄 Geolocation features
- 🔄 Advanced analytics
- 🔄 PWA capabilities
- 🔄 Multi-language support

---

*এই ডকুমেন্টেশন নিয়মিত আপডেট করা হয় প্রকল্পের উন্নতির সাথে সাথে।*